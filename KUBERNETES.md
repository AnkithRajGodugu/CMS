# Kubernetes Cluster Setup

This file describes how to deploy the project to a Kubernetes cluster.

## Prerequisites
- Kubernetes cluster (local or cloud)
- kubectl installed

## Deployment Steps
1. Create deployment and service YAML files for backend and frontend
2. Apply configurations using `kubectl apply -f <file>.yaml`
3. Verify pods and services are running
